# 配置模板 —— 复制为 .env 后填入真实值（.env 已被 .gitignore 排除，永不入库）
#   cp .env.example .env
# 真实连接串/密钥只放 .env；本文件保持占位符，可安全提交。
# 另有 pre-commit 钩子（githooks/）兜底拦截误提交的 .env* 文件。

# ── 团队功能：中心数据库（MySQL 协议：TiDB / MySQL / 云实例）──
# 未配置 = 本地模式，团队功能自动禁用，个人数据仍走本地 SQLite
# 云实例（TiDB Cloud 等）强制 TLS，结尾必须带 ?ssl={}（空 JSON 对象；
# 写 ?ssl=true 会报 "SSL profile must be an object"）
# 密码含特殊字符需 URL 百分号编码：@ -> %40  : -> %3A  / -> %2F  % -> %25  # -> %23
# DB_URL=mysql://user:password@host:4000/autotest_team?ssl={}

# ── 团队功能：中心 JWT 签名密钥（生产 + DB_URL 时必填）──
# openssl rand -hex 32
# TEAM_JWT_SECRET=

# ── 本地实例 JWT 密钥（生产建议设置；不设则开发模式用内置开发密钥）──
# openssl rand -hex 32
# JWT_SECRET=

# ── Agent SSH 凭据加密密钥（32 字节 hex，用于 devices 表 SSH 密码/私钥列的 AES-256-GCM 加密）──
# openssl rand -hex 32
# AGENT_SSH_KEY_SECRET=

# ── 可选 ──
# PORT=3000
# DATA_DIR=./data
# CORS_ORIGINS=https://center.example.com,http://localhost:3000
